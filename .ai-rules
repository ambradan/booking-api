# AI Agent Rules (Futurecode)

## Project Memory
Generated: 2026-02-20 10:10 UTC
Decisions: 5 active | Rejections: 0 | Entities: 38 tracked

## Active Decisions
- **Prisma as ORM** — [auto-inferred] Project imports prisma
- **Environment variables for config** — [auto-inferred] Found .env in project
- **Docker Compose for orchestration** — [auto-inferred] Found docker-compose.yml in project
- **Node.js runtime** — [auto-inferred] Found package.json in project
- **TypeScript as language** — [auto-inferred] Found tsconfig.json in project

## Entity Graph
- dependency: 14
- module: 12
- class: 7
- function: 4
- package: 1

If your task conflicts with any decision above, STOP and ask before proceeding.

## Mandatory Rules

Type safety:
- No `as any`. Use `as unknown` + type guard.
- No blanket `# type: ignore`. Use `# type: ignore[specific-code]`.
- No `@ts-ignore`. Use `@ts-expect-error`.

Error handling:
- No bare `except:`. No `except Exception: pass`. No empty catch blocks.
- Always log or handle errors.

Security:
- No `eval()`, `exec()`, `shell=True`, SQL concatenation, `innerHTML =`.

Architecture:
- No circular imports. No module-level mutable state.
- Hard-to-reverse decisions → ask human first.
- Every hardcoded value → ASSUMPTION comment with VALID_UNTIL and RISK_IF_WRONG.

Verification: run `futurecode analyze . --voice=plain` and `futurecode lint .` before committing.
